server:
  port: 8082

spring:
  application:
    name: operaciones-service

  datasource:
    url: jdbc:postgresql://operaciones-db:5432/opsdb
    username: admin
    password: admin
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true