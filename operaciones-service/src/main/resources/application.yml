spring:
  application:
    name: operaciones-service
  datasource:
    url: jdbc:postgresql://localhost:5433/operaciones_db
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate.format_sql: true
server:
  port: 8082
server:
  port: 8082

spring:
  application:
    name: operaciones-service

  datasource:
    url: jdbc:postgresql://operaciones-db:5432/opsdb
    username: admin
    password: admin
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true